# KHÓA BÍ MẬT — TUYỆT ĐỐI KHÔNG commit
.env
.env.*
!.env.example

# Phụ thuộc & build
node_modules/

# Database state (giữ trên persistent disk của Render, không đẩy git)
*.sqlite
*.sqlite-*
data/

# Hệ thống
.DS_Store
npm-debug.log*
*.log
.bak/

# Script chạy trên MÁY MAC, chứa ADMIN_TOKEN thật — KHÔNG đẩy lên GitHub (chốt 20/07)
scripts/phu-voucher-hang-ngay.sh
logs/
scripts/.pancake-stub.mjs
